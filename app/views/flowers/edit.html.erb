<% provide(:title, "Edit") %>
<h1 class="nice_heading">Discuss <%= @flower.name %></h1>
<hr>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for(@flower) do |f| %>
		 	<%= f.text_area :discussion, rows: 10, class: "form-control" %>
      <%= f.submit "Update", class: "btn btn-lg btn-primary" %>
		<% end %>
	</div>
</div>
